# 贡献指南

欢迎贡献新机型适配、桌面兼容性修复、内核支持和文档改进。可以让 Claude Code、Codex 等编程助手阅读 [AGENTS.md](AGENTS.md) 后协助完成。

## 新机型支持

从 [机型适配指南](docs/hardware-adaptation.md) 开始，先确认设备接口和状态数据，再整理代码。一次 PR 尽量围绕一个机型或一项兼容性问题，复用已有事件传递、显示与安装机制。

## 修改与测试

1. 从最新 `main` 创建工作分支，保留现有工作区中的未提交修改。
2. 完成改动。涉及机型匹配时同步检查内核驱动和绑定脚本；涉及事件解码时加入对应的离线数据测试。
3. 按改动范围验证：文档检查链接和命令，Python 行为运行 `make test`，驱动使用对应内核头文件编译并在目标机加载测试。
4. 按键功能通过实体操作核对状态和图标。更新 [测试与兼容性](docs/validation.md)，注明实际验证环境和待测项目。

## 提交 PR

目标仓库：<https://github.com/wearzdk/mechrevo-osd-linux>，目标分支：`main`。没有写入权限时，通过自己的 fork 提交 PR。

AI 助手在用户明确要求贡献时，可以使用已配置的 Git/GitHub 身份完成提交、推送和 PR 创建。缺少账号或认证时，将需要用户完成的步骤说明清楚，并保留已准备的改动。

PR 使用仓库提供的模板，说明：

- 解决的问题与修改后的表现。
- 适用机型、内核和桌面环境。
- 自动测试、模块构建及实体按键测试结果。
- 安装/恢复验证情况与尚未覆盖的项目。

仅与文档有关的 PR 可将硬件测试填写为“不适用”。测试材料保留复现所需信息即可；图标归属见 [NOTICE](NOTICE)。
